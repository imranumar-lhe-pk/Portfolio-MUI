import { createTheme } from "@mui/material/styles";
// import { saddlebrown } from "@mui/material/colors";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F8F2FF",
    },
    secondary: {
      main: "#E7EFF4",
    },
  },
});

export default theme;
